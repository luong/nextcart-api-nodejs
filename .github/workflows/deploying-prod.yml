name: Deploying Prod

on:
  workflow_dispatch:
  push:
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
    
jobs:
  build:
    environment: production
    runs-on: ubuntu-latest
    steps:
      - run: echo 'TODO'
